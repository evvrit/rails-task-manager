<p><%= link_to "<home", tasks_path %></p>
<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed ? '✅' : '' %>
<p><%= link_to 'Update', edit_path(@task) %></p>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
